# Docker

Deployment assets for the Cloudflared dashboard.

Planned:

- `Dockerfile.web` — image untuk `apps/web` (Next.js standalone output)
- `docker-compose.yml` — service `web` + `cloudflared`, dengan mount Docker socket
